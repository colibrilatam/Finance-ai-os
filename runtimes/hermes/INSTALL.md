# Hermes

Copia/mezcla `config.yaml` en `~/.hermes/config.yaml`, conserva el filtrado de herramientas y ejecuta `hermes chat`.
El MCP HTTP debe estar visible para Hermes; si Hermes corre en otro host, reemplaza localhost por el FQDN/TLS interno.
